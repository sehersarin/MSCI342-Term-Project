// Create stub which returns an array of Worker ID's given an input of school ID or school Name
async function inputSchool(schoolID, schoolName) {
    
    var did_it_work = false;
          //* insert (schoolID or schoolName)
    did_it_work = true;
          var workerID=[8000000,8000001,8000002,8000003]
    return did_it_work;
  };
  
  module.exports = {
    inputSchool,
  }