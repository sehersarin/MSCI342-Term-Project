async function userAccount(firstName, lastName, type, studentID, email, password, phone) {
    const user = {
       //create a dummy user
    };

    return user;
};


module.exports = {
    userAccount,
    isAccessTokenValid,
} 